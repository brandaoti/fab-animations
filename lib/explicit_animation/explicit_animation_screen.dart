import 'package:flutter/material.dart';

class ExplicitAnimationScreen extends StatelessWidget {
  const ExplicitAnimationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
